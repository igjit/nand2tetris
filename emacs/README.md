My Emacs configuration for Jack language.

- major-mode
- flycheck

## Usage

Add the `tools` directory to Emacs `exec-path`.

```el
(add-to-list 'exec-path "path/to/nand2tetris/tools")
```

And load `jack.el`
